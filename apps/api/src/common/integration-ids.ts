import { DEFAULT_WORKPLACE_ID } from './request-user';

export { DEFAULT_WORKPLACE_ID as DEFAULT_WORKSPACE_ID };

/** Stable dev user IDs — match Workplace Platform workspace members when integrated. */
export const DEV_USER_IDS = {
  rahul: 'usr-dev-rahul',
  priya: 'usr-dev-priya',
  arjun: 'usr-dev-arjun',
  sachin: 'usr-dev-sachin',
  ananya: 'usr-dev-ananya',
} as const;
